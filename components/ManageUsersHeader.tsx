export function ManageUsersHeader() {
  return (
    <div className="usa-prose margin-bottom-3">
      <h1>Manage users</h1>
      <p className="usa-prose-intro">
        Add and remove users from an org. Add users to spaces and set access
        levels using role.
      </p>
    </div>
  );
}
